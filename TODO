## TODO

- [x] 讓 polling model 變成 template，所有 service 都可以變成一個 Worker 的 subclass
    - 擁有雙邊 polling 行為，我想看到的設計是下列或是同等邏輯更好的寫法

```cpp
while (g_running)
{
#ifdef PRODUCTION_MODE
    worker->poll_client();
    worker->poll_server();
    POLL_BACKOFF(); 
#else
    int has_task = 0;
    has_task |= worker->poll_client(); // 0 or 1
    has_task |= worker->poll_server(); // 0 or 1
    if (!has_task) {
        POLL_BACKOFF(); 
    }
#endif
}
```
    
- [ ] **Price**
    - i64 price -> struct Price { i64 m; i8 e; }; (price = m * 10^e)
    - 要和 fbs 良好整合

- [ ] **SymbolTable**
- [ ] **分離責任** requires **Price** **SymbolTable**
    - 讓 client manager 處理所有 multi-client parallelizable 任務
    - 讓 matching engine 處理所有 multi-symbol parallelizable 任務
    - 現狀可能是有點混在一起